# This is a placeholder for your release keystore
# Generate your actual keystore using:
# keytool -genkey -v -keystore release.keystore -alias your_key_alias -keyalg RSA -keysize 2048 -validity 10000

# Update the signing config in build.gradle.kts with your actual keystore details